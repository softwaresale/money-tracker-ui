import { SubpageContainer } from './subpage-container';

describe('SubpageContainer', () => {
  it('should create an instance', () => {
    const directive = new SubpageContainer();
    expect(directive).toBeTruthy();
  });
});
