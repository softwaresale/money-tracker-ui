import { CenteredContent } from './centered-content';

describe('CenteredContent', () => {
  it('should create an instance', () => {
    const directive = new CenteredContent();
    expect(directive).toBeTruthy();
  });
});
