import { ActionBox } from './action-box';

describe('ActionBox', () => {
  it('should create an instance', () => {
    const directive = new ActionBox();
    expect(directive).toBeTruthy();
  });
});
